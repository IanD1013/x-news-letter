# Local pipeline options (all optional)
BACKFILL_PAGES=2

# Repository the site edits when you follow or unfollow accounts from the UI (owner/name).
# The workflow sets this automatically; only needed for `npm run dev`.
VITE_REPO=

# Notification test (scripts/notify.ts)
NTFY_TOPIC=
NTFY_SERVER=https://ntfy.sh
SITE_URL=http://localhost:4173/
NEW_IDS=
